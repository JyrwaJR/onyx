# ============================================================
# Model & Generate — listing models and generating text.
# Docs: https://opencode.ai/v2/docs/api
# ============================================================


### List models
GET {{BASE_URL}}/api/model

### Get default model
GET {{BASE_URL}}/api/model/default

### Generate text
POST {{BASE_URL}}/api/generate
Content-Type: application/json

{
  "messages": [
    {
      "role": "user",
      "content": "Hello"
    }
  ],
  "model": "gpt-4o"
}
